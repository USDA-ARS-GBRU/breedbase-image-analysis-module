openapi: 3.0.0
info:
  title: Image Upload API
  version: "1.0"

paths:
  /upload:
    post:
      summary: Upload an image and run analysis.
      operationId: api.app.upload_image_and_process

      parameters:
        - in: query
          name: output_mode
          required: false
          schema:
            type: string
            enum: [single, all]
            default: single
          description: >
            Controls which traits are included in the response while keeping the same response structure.
            "single" returns only one trait per object (POC mode). "all" returns all supported traits.

      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [image]
              properties:
                image:
                  type: string
                  format: binary
            encoding:
              image:
                contentType: image/*

      responses:
        '200':
          description: File processed successfully.
          content:
            application/json:
              schema:
                type: object
                required: [job_id, pipeline, qc, objects]
                properties:
                  job_id: { type: string }
                  timestamp: { type: string }
                  pipeline:
                    type: object
                    properties:
                      name: { type: string }
                      version: { type: string }
                  qc: { type: object }
                  output_mode:
                    type: string
                    enum: [single, all]
                  traits_emitted:
                    type: array
                    items: { type: string }
                  derived_images:
                    type: array
                    items:
                      type: object
                      properties:
                        role: { type: string }
                        filename: { type: string }
                        url: { type: string }
                  objects:
                    type: array
                    items:
                      type: object
                      required: [object_id, traits]
                      properties:
                        object_id: { type: string }
                        source_label: { type: string }
                        bbox: { type: object }
                        qc: { type: object }
                        traits:
                          type: object
                          additionalProperties:
                            type: object
                            properties:
                              value: { nullable: true }
                              unit: { nullable: true }

        '400':
          description: Missing image or wrong type.
